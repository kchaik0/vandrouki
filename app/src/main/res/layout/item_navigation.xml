<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android">

    <data>

        <import type="android.view.View" />

        <variable
            name="loadUrlService"
            type="kchaiko.vandrouki.network.service.LoadUrlServiceKt" />

        <variable
            name="page"
            type="java.lang.Integer" />

        <variable
            name="handler"
            type="kchaiko.vandrouki.items.NavigationItem.ClickHandler" />

    </data>

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginBottom="20dp"
        android:layout_marginTop="20dp"
        android:gravity="center_horizontal"
        android:orientation="horizontal">

        <Button
            android:id="@+id/prevPage"
            style="?android:attr/buttonBarStyle"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginEnd="8dp"
            android:background="@drawable/nav_arrow_left"
            android:onClick="@{handler::onClick}" />

        <Button
            android:id="@+id/nextPage"
            style="?android:attr/buttonBarStyle"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginStart="8dp"
            android:background="@drawable/nav_arrow_right"
            android:onClick="@{handler::onClick}"
            android:visibility="@{page > loadUrlService.DEFAULT_PAGE ? View.VISIBLE : View.GONE}" />

    </LinearLayout>

</layout>